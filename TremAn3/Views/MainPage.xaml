<Page
    x:Class="TremAn3.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModels="using.TremAn3.ViewModels"
    xmlns:toolkitConverters="using:Microsoft.Toolkit.Uwp.UI.Converters"
    xmlns:oxyplot="using:OxyPlot.Windows"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    Style="{StaticResource PageStyle}"
    Loaded="{x:Bind ViewModel.LoadedAsync}"
    mc:Ignorable="d">
    <Page.Resources>
        <toolkitConverters:StringFormatConverter x:Key="StringFormatConverter"></toolkitConverters:StringFormatConverter>
    </Page.Resources>
    <SplitView
            x:Name="MySplitView"
           DisplayMode="Inline"
           OpenPaneLength="200"
           PanePlacement="Right"
           IsPaneOpen="{x:Bind ViewModel.IsFreqCounterOpen, Mode=OneWay}"
        >
        <SplitView.Pane>
            <Grid
                x:Name="SplitViewArea">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Button  Click="{x:Bind ViewModel.GetFrameClickAsync}" >Get curent frame</Button>

                <TextBlock Grid.Row="1"><Run>VideoMainFreq:  </Run> <Run Text="{x:Bind ViewModel.VideoMainFreq, Mode=OneWay}"/></TextBlock>
                <Grid  Grid.Row="2" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <TextBlock  >
                    <Run Text="{x:Bind ViewModel.ProgressPercentage, Mode=OneWay,Converter={StaticResource StringFormatConverter}, ConverterParameter=' {0:F2} %' }" />
                        <!--<Run Text=" %"/>-->
                    </TextBlock>
                    <ProgressBar Grid.Column="1" MinWidth="0"   Value="{x:Bind ViewModel.ProgressPercentage, Mode=OneWay}" />
                </Grid>
                <oxyplot:PlotView  Grid.Row="3" Width="200" Height="200" Model="{x:Bind ViewModel.FreqCounterViewModel.PlotModel, Mode=OneWay}"/>
                <Grid Grid.Row="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <controls:RangeSelector  x:Name="RangeSelectorControl" Width="180" Grid.ColumnSpan="2"
	                    Minimum="0" 
	                    Maximum="{x:Bind ViewModel.FreqCounterViewModel.Maximum, Mode=OneWay}"
                        RangeMax="{x:Bind ViewModel.FreqCounterViewModel.Maxrange, Mode=TwoWay}"
                        RangeMin="{x:Bind ViewModel.FreqCounterViewModel.Minrange, Mode=TwoWay}">
                    </controls:RangeSelector>
                    <TextBlock Grid.Row="1" Text="{x:Bind ViewModel.FreqCounterViewModel.DoubleToTimeConverter(ViewModel.FreqCounterViewModel.Minrange), Mode=OneWay}"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" TextAlignment="Right" Text="{x:Bind ViewModel.FreqCounterViewModel.DoubleToTimeConverter(ViewModel.FreqCounterViewModel.Maxrange), Mode=OneWay}"/>
                </Grid>
            </Grid>
        </SplitView.Pane>

    <Grid
        x:Name="ContentArea"
        Margin="{StaticResource MediumLeftRightMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

        <!--<StackPanel Orientation="Horizontal">-->
                <!--<Button Click="{x:Bind ViewModel.GetFrameClickAsync}" >Get curent frame</Button>-->
                <!--<Button Content="Load Video" Click="{x:Bind ViewModel.OpenVideo_ButtonClickAsync}"/>-->
                <!--<Image Source="{x:Bind ViewModel.CurrentFrameSource, Mode=OneWay}" ></Image>-->
                <!--<Button x:Name="btnShowPane" HorizontalAlignment="Right" VerticalAlignment="Top" Width="48" FontFamily="Segoe MDL2 Assets" Content="&#xE700;" Click="btnShowPane_Click" />-->

            <!--</StackPanel>-->
          

        <MediaPlayerElement
            Grid.Row="1" x:Name="mpe" AutoPlay="False" AreTransportControlsEnabled="True"
            Source="{x:Bind ViewModel.MediaPlayerViewModel.Source, Mode=OneWay}" >
            <MediaPlayerElement.TransportControls>
                <MediaTransportControls IsCompact="False"/>
            </MediaPlayerElement.TransportControls>
        </MediaPlayerElement>
        </Grid>
    </SplitView>
</Page>
