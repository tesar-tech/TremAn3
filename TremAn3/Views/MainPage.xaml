<Page
    x:Class="TremAn3.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModels="using.TremAn3.ViewModels"
    xmlns:toolkitConverters="using:Microsoft.Toolkit.Uwp.UI.Converters"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">
    <Page.Resources>
        <toolkitConverters:StringFormatConverter x:Key="StringFormatConverter"></toolkitConverters:StringFormatConverter>
    </Page.Resources>
    <SplitView
            x:Name="MySplitView"
           IsPaneOpen="False"
           DisplayMode="CompactInline"
           OpenPaneLength="200"
           PanePlacement="Right">
        <SplitView.Pane>
            <Grid
                x:Name="SplitViewArea">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Button x:Name="btnShowPane" HorizontalAlignment="Right" VerticalAlignment="Top" Width="48" FontFamily="Segoe MDL2 Assets" Content="&#xE700;" Click="btnShowPane_Click" />
                <TextBlock Grid.Row="1"><Run>VideoMainFreq:  </Run> <Run Text="{x:Bind ViewModel.VideoMainFreq, Mode=OneWay}"/></TextBlock>
                <StackPanel Orientation="Horizontal" Grid.Row="2" >
                    <TextBlock Text="{x:Bind ViewModel.ProgressPercentage, Mode=OneWay,Converter={StaticResource StringFormatConverter}, ConverterParameter='0:0.00' }" />
                    <ProgressBar  HorizontalAlignment="Stretch" Value="{x:Bind ViewModel.ProgressPercentage, Mode=OneWay}" />
                </StackPanel>
            </Grid>
        </SplitView.Pane>

    <Grid
        x:Name="ContentArea"
        Margin="{StaticResource MediumLeftRightMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
                <!--<Button Click="{x:Bind ViewModel.GetFrameClickAsync}" >Get curent frame</Button>-->
                <Button Click="{x:Bind ViewModel.GenRanNum}" >Get curent frame</Button>
                <Button Content="Load Video" Click="{x:Bind ViewModel.OpenVideo_ButtonClickAsync}"/>
                <!--<Image Source="{x:Bind ViewModel.CurrentFrameSource, Mode=OneWay}" ></Image>-->
        </StackPanel>
            <Grid
            Grid.Row="2"
            Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <!--The SystemControlPageBackgroundChromeLowBrush background represents where you should place your content. 
                Place your content here.-->
        </Grid>

        <MediaPlayerElement
            Grid.Row="2" x:Name="mpe" AutoPlay="False" AreTransportControlsEnabled="True"
            Source="{x:Bind ViewModel.MediaPlayerViewModel.Source, Mode=OneWay}" >
            <MediaPlayerElement.TransportControls>
                <MediaTransportControls IsCompact="False"/>
            </MediaPlayerElement.TransportControls>
        </MediaPlayerElement>
    </Grid>
    </SplitView>
</Page>
